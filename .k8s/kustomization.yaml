apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml

commonLabels:
  app.kubernetes.io/name: happytest-reader
  app.kubernetes.io/component: data-collector

images:
  - name: ghcr.io/your-org/happytest/reader
    newTag: latest
